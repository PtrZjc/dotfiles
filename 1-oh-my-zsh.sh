#!/bin/sh
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

echo 'Before running 2nd script, install homebrew and 4 Meslo fonts from ./font folder (although they may be installed with p10k theme with iTerm2'
